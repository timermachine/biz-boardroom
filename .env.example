# Copy to .env and fill in values
# cp .env.example .env

# Registry — set to your GitHub org/user + repo
GITHUB_REPOSITORY=your-org/myapp
TAG=latest

# App
NODE_ENV=development
PORT=3000

# Database
POSTGRES_USER=myapp
POSTGRES_PASSWORD=changeme
POSTGRES_DB=myapp
